---
description: Bootstrap the orchestrator and enter the main SDLC loop
---
Bootstrap per docs/00-handover.md STEP 1-8 (read the bootstrap doc set,
read project-profile.yaml and modules-enabled.yaml, run the recovery
procedure). Then report the board snapshot to the human in 繁體中文
(counts per state, awaiting gates, blocked, open HUMAN-INPUTs) and enter
the main loop (CLAUDE.md priority order) until limits.yaml wrap-up
conditions are met. End with a session report.
